# square_core -- the pipeline core
# gazu>=0.10 previously had no upper bound -- square_core.kitsu.auth reads
# token shapes specific to how 1.2.x's KitsuClient actually stores them
# (a .tokens dict from log_in(), separate .access_token/.refresh_token
# attributes only from refresh_access_token()); an untested older/newer
# major version could differ and silently break login. Pin to what's
# actually verified.
gazu>=1.2,<2
requests>=2.31.0
pillow>=10.0.0
xxhash>=3.3.0
imageio-ffmpeg>=0.4.8
OpenImageIO>=2.4
pyyaml>=6.0

# desktop tools (ingest_tool, config_editor, review_player, ...) additionally
# need these. square_core itself never imports Qt -- one requirements file for
# now (no core/tools split until that's actually needed).
Qt.py>=1.3.0
PySide6>=6.5.0
