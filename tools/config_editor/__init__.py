"""Square config editor -- the admin tool that is the only writer of studio and
project config.

    python -m tools.config_editor            # Qt GUI
    python -m tools.config_editor --cli ...  # headless get / set / list / diff

See `docs/config_schema.md` and `tools/config_editor/core/editor.py`.
"""
